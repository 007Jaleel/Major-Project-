# Backend Configuration
PORT=3000
NODE_ENV=development
JWT_SECRET=your_jwt_secret_key_here

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=john@mysql9747639192
DB_NAME=educonnect
DB_PORT=3306

# PostgreSQL (if using)
PG_HOST=localhost
PG_USER=postgres
PG_PASSWORD=postgres
PG_DATABASE=educonnect
PG_PORT=5432

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Frontend Configuration
## Frontend API origin (OPTIONAL)
#
# If set, it must be ONLY the API ORIGIN (scheme + host + port), e.g.:
#   http://localhost:3000
#
# Do NOT include `/api` or `/api/v1` here because the frontend already calls
# endpoints like `/api/v1/auth/login` and Vite dev proxy forwards `/api/*`.
VITE_API_URL=http://localhost:3000
